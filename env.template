# Application
NODE_ENV=development
PORT=3000

# Bybit API (Optional - improves rate limits)
BYBIT_API_KEY=
BYBIT_API_SECRET=
BYBIT_TESTNET=false

# Discord - Webhook (DEJA CONFIGURAT)
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/1467838117645127837/PFHUHID5NspB5soxlqBgSTeRgq5ZD1KN5XGdZNtbPXJYk3bELCQ5erbiTRMfOuFGZgHB

# PostgreSQL - Local
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=dynasty_bot
POSTGRES_SSL=false

# Redis - Local
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# Bot Configuration
MIN_SCORE_THRESHOLD=75
COOLDOWN_SECONDS=300
MAX_ALERTS_PER_HOUR=20
ENABLE_BTC_CONTEXT=true

# Quality Filters for USDT Perpetual Tokens
# Bot monitors tokens that meet ALL criteria:
# - Listed on Bybit as USDT perpetuals
# - Volume 24h ≥ $1M (adjustable - lower = more pairs)
# - Open Interest ≥ $500k (adjustable - lower = more pairs)
# - Not in blacklist
# - Have sufficient funding rate data
MIN_VOLUME_24H_USDT=1000000         # Minimum 24h volume in USDT ($1M default, lower = more pairs)
MIN_OPEN_INTEREST_USDT=500000       # Minimum open interest in USDT ($500k default, lower = more pairs)
MIN_PRICE_USDT=0.0001               # Minimum price to avoid dust coins
MAX_PRICE_USDT=100000               # Maximum price to avoid very expensive coins

# Blacklist - Symbols to exclude (comma-separated, case-insensitive)
# Example: BLACKLIST_SYMBOLS=BTCUSDT,ETHUSDT,DOGEUSDT
BLACKLIST_SYMBOLS=